import { withInstall } from '~/_utils';
import MarkdownRenderer from './MarkdownRenderer.vue';

export const VMarkdownRenderer = withInstall(MarkdownRenderer);
export default VMarkdownRenderer;

export * from './types';
